import {PopoverTheme} from './Popover';

export const popoverTheme: PopoverTheme = {
    root: {
        base: ''
    },
    overlay: 'fixed inset-0 bg-gray-900 bg-opacity-50 dark:bg-opacity-80 transition-opacity top-[104px]'
}